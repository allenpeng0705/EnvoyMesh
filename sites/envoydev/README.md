# EnvoyDev site

Drop the EnvoyDev marketing site files in this folder so the EnvoyMesh homepage link `envoydev/` resolves.

Expected entry: `index.html` (and any assets the EnvoyDev site needs).

Do not mirror EnvoyMesh versioning here — EnvoyDev keeps its own site version.
