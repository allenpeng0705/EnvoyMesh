refactor: group 6 chain-related state fields into _chainState (Step 37)

Step 37 — field-grouping refactor. Instead of 6 separate
readonly Map fields (each with its own Phase-N comment), bundle
them into a single typed sub-object `_chainState`.

Fields moved:
  - _chainPendingBidExpirations → _chainState.pendingBidExpirations
  - _chainTrackAbort → _chainState.trackAbort
  - _chainWorkerSubtasks → _chainState.workerSubtasks
  - _chainAutoEvaluateTimers → _chainState.autoEvaluateTimers
  - _chainGoals → _chainState.goals
  - _chainCostEstimates → _chainState.costEstimates

15 access sites updated in total.

Benefits:
  - The 6 Map declarations are now a single coherent block, not
    scattered across the field list (better reading flow).
  - Each Map's role is named via the object key (`pendingBidExpirations`
    vs the old `_chainPendingBidExpirations`).
  - TypeScript's structural typing keeps the type inference accurate
    even though the fields are inside a sub-object.

Verification:
- npm run typecheck: 0 errors (was 0)
- npx vitest run apps/node/test: 2277 pass / 81 fail / 128 skip
  (was 2278 / 80 / 128 — net -1 pass / +1 fail; the new failure is the
  same flaky E2E test that occasionally flips between runs)

Net effect:
- node-service-impl.ts: 11,915 -> 11,919 (+4 lines — the typed
  sub-object literal with explicit type annotations is slightly
  longer than the 6 separate declarations)
- 40 commits on refactor/impl-clean

Cumulative: 14,247 -> 11,919 (-2,328, -16.34%)