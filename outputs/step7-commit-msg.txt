refactor: extract chains module (Phase 40) — partial

Step 7 of the node-service-impl.ts extraction plan. The chains
section is large (~1189 lines), so this commit extracts the state
and 10 simple delegations. The complex methods (chainPlan,
chainLaunch, chainEvaluateBids, chainRebalance, chainCounterBid,
chainPreviewGoal, chainStartFromGoal, chainExportCosts,
chainListRecipes, chainSaveRecipe, chainDeleteRecipe, etc.) still
live on the class — they pull in many other class helpers and
extracting them is out of scope for this commit.

- New runtime module: apps/node/src/node-service-chains.ts (302 lines)
  - ChainStore class:
    - getRuntime / setRuntime / listActive / listIds / clear
    - ensureRuntime (placeholder creation)
    - cancel(chainId, subtaskId?)
    - getBidStrategy / setBidStrategy (with default fallback)
  - 10 high-level operations:
    - chainGetStateViaRuntime, chainListActiveViaRuntime,
      chainCancelViaRuntime
    - chainListReportsViaRuntime, chainGetReportViaRuntime,
      chainPinReportViaRuntime
    - chainSetBidStrategyViaRuntime,
      chainGetBidStrategyViaRuntime
    - chainGetDefaultsViaRuntime,
      chainSetDefaultsViaRuntime (with validation)
  - ChainContext { store, hasTaskStore, listChainReports,
    getChainReport, pinChainReport, getChainGoal,
    getChainCostEstimate, snapshotToResult, bidsBySubtask,
    getNodeConfig, setNodeConfig }
  - ChainBidStrategy interface (was inline in the class)
- Class fields: `chainRuntime` + `chainBidStrategies` Maps replaced
  with single `_chainStore` field.
- Class methods: 10 simple methods collapsed to 1-line delegations.
- Complex methods (chainPlan, chainLaunch, etc.) updated to access
  state via `this._chainStore.getRuntime / setRuntime / listActive`.
- node-service-impl.ts: 13849 -> 13778 lines (-71)
- 13 new unit tests cover:
  - ChainStore: getRuntime, setRuntime+getRuntime, cancel full
    chain, cancel single subtask, cancel unknown chain, bid
    strategy default + set/get, listActive, listIds, clear
  - chainGetState empty-state shape
  - chainCancel empty-state
  - chainSetBidStrategy + chainGetBidStrategy round-trip

Verification:
- npm run typecheck: 0 errors (was 0)
- npx vitest run apps/node/test: 2124 pass / 79 fail / 128 skip
  (was 2110 / 79 / 128 — +13 new tests, 0 regressions)

Note: chainPlan / chainLaunch / chainRebalance / chainCounterBid /
chainPreviewGoal / chainStartFromGoal / chainExportCosts /
chainListRecipes / chainSaveRecipe / chainDeleteRecipe and the
inbound-envelope + capability-index paths still live on the class
and read state via the store. They are good candidates for
follow-up commits if the user wants them extracted.