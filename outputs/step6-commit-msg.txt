refactor: extract continuity module (Phase 25)

Step 6 of the node-service-impl.ts extraction plan. The continuity
section turned out to be smaller than estimated (~178 lines, not
~1000) — most of that range was unrelated features. But the
extraction is the same shape as Step 5: a `ContinuityStore` class
that owns the in-memory list + JSON file, plus 4 high-level
operations that wrap the underlying `continuity-service` module.

- New runtime module: apps/node/src/node-service-continuity.ts (169 lines)
  - ContinuityStore class:
    - loadFromDisk() — drops malformed entries, returns the list
    - list() — defensive copy
    - upsert(session) — replace by id + persist
    - setAll(sessions) — overwrite + persist
    - clear() — drop in-memory entries
  - 4 high-level operations:
    - startContinuitySessionViaRuntime(ctx, description, opts?)
    - updateContinuitySessionViaRuntime(ctx, sessionId, update)
    - completeContinuitySessionViaRuntime(ctx, sessionId)
    - getResumableSessionsViaRuntime(ctx)
  - buildContinuityFilePath(profileDir) helper
  - ContinuityContext { store, getDeviceId }
- Class field replaced: 3 private methods + 4 public methods
  collapsed into 1 store field + 1-line delegations + a
  `_continuityContext()` factory.
- node-service-impl.ts: 13965 -> 13849 lines (-116)
- 17 new unit tests cover:
  - file-path helper
  - ContinuityStore: missing file, malformed entries dropped,
    defensive copy, upsert replace-by-id, upsert persists to disk,
    setAll overwrites, clear(), no-file no-op
  - start: deviceType application, getDeviceId() fallback
  - update: happy path + null on miss
  - complete: marks inactive + no-op on miss
  - resumable: sorted by lastUpdatedAt desc, inactive filtered out

Verification:
- npm run typecheck: 0 errors (was 0)
- npx vitest run apps/node/test: 2110 pass / 79 fail / 128 skip
  (was 2094 / 79 / 128 — +17 new tests, 0 regressions;
  pre-existing fleet-manifest flake passed in one run + failed in
  another — same flake we saw in Step 3)