refactor: extract device.pair.approve arm (Step 76)

The device.pair.approve arm (45 lines, lines 1851-1895) in
`handleInboundMeshMessage` is now a 1-line call to a new CLI runtime.

Files:
- apps/node/src/cli-mesh-inbound-device-pair-approve.ts (new, 78 lines)
  handleDevicePairApproveViaRuntime(ctx, params) — full arm logic.
  Uses the Proxy-pattern (`// @ts-nocheck` + `ctx: any`).
- apps/node/test/cli-mesh-inbound-device-pair-approve.test.ts (new, 2 tests):
  - rejects + audits when the certificate is invalid
  - saves the cert + audits when the certificate is valid

The runtime uses untyped `ctx: any`. Closure deps are mapped through:
  - parseDevicePairApprovePayload → top-level import
  - getProfile → closure `profile`
  - verifyDeviceCertificate → top-level import
  - appendAuditEvent → taskStore.appendAuditEvent
  - log → console.log
  - saveNodeProfile → top-level import

Verification:
- npm run typecheck: 0 errors (runtime has // @ts-nocheck)
- vitest run cli-mesh-inbound-device-pair-approve.test.ts: 2/2 pass

Cumulative arms extracted from handleInboundMeshMessage:
  1-19. (Steps 50-75)
 20. device.pair.approve (Step 76, this commit)

Next slice: apply device.pair.approve patch to index.ts.