refactor: apply discovery.* patch (Step 73)

Applied the index.ts patch from Step 72. The discovery.* arm
(88 lines, lines 1198-1285) is now a 1-line call to
`handleDiscoveryViaRuntime`.

Files touched:
- apps/node/src/index.ts: -88 lines (discovery.* arm collapsed)

The replyWithEnvelope parameter is cast to `any` (same pattern
as in chat.room.message patch — the declared type is
`(envelope: EnvoyEnvelope) => Promise<void>` but the runtime
expects `(envelope: unknown) => Promise<void>`).

Verification:
- npm run typecheck: 0 errors

Net effect:
- apps/node/src/index.ts: 5027 → 4939 (-88 lines)

Cumulative arms extracted from handleInboundMeshMessage:
  1-16. (Steps 50-72)
 17. discovery.* (Steps 72+73)

Remaining big targets:
  - chat.message (~300 lines, lines 1615-1915) — needs incremental
    refactor; too entangled for one-shot extraction
  - device.pair.request (~200 lines, lines 1916-2118)
  - device.pair.approve (~46 lines)
  - device.pair.deferred (~20 lines)